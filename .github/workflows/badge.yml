# Dummy workflow to display a single "build passing" badge in the main README.md

name: build

on:
  pull_request:
    types: [closed]
    branches:
      - master

jobs:
  noop:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged
    steps:
      name: finalize
      if: github.event.pull_request.merged
      run: |
        echo "Successfully merged into master."
